apiVersion: v1
kind: Pod
metadata:
  name: debug # pod name
  namespace: roboshop
  labels:
    project: roboshop
    environment: dev
    purpose: debug
spec:
  containers:
  - name: debug # container name
    image: murali9686/debug:2.0.0 # by default it will get from docker hub
    
    
  